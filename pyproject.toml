[project]
name = "wechat-engineering-agent"
version = "0.1.0"
description = "WeChat Engineering Intelligence Platform"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "sqlalchemy",
    "alembic",
    "pgvector",
    "celery",
    "redis",
    "typer"
]
